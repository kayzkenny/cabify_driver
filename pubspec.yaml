name: cabify_driver
description: A driver companion app for cabify.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.2
  geolocator: ^6.1.14
  google_fonts: ^1.1.1
  connectivity: ^2.0.2
  firebase_core: ^0.7.0
  audioplayers: ^0.16.1
  maps_toolkit: ^1.1.0+2
  cupertino_icons: ^1.0.0
  firebase_auth: ^0.20.0+1
  firebase_storage: ^7.0.0
  cloud_firestore: ^0.16.0
  flutter_riverpod: ^0.12.2
  animated_text_kit: ^3.1.0
  google_maps_flutter: ^1.1.1
  flutter_polyline_points: ^0.2.4
  geoflutterfire:
    git:
      url: https://github.com/awaik/GeoFlutterFire

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - images/
    - sounds/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
